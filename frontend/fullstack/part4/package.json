{
  "name": "part4",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf build && cd ../part2/ && npm run build && cp -r build  ../part4/",
    "test": "NODE_ENV=test jest --verbose --runInBand"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express-async-errors": "^3.1.1",
    "jest": "^29.3.1",
    "nodemon": "^2.0.20",
    "supertest": "^6.3.1"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^8.5.1",
    "mongodb-memory-server": "^8.10.0",
    "mongoose": "^6.7.2",
    "mongoose-unique-validator": "^3.1.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
